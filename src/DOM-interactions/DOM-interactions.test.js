import { grabName, CreateBoat, hit, isSunk, Gameboard } from "../functions _&_tests/functions";
import { exampleAdd } from "./DOM-interactions"

test("test", ()=>{
    expect(exampleAdd(2,3)).toBe(5);
});